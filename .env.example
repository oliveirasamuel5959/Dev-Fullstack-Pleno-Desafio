# Template — copie para .env e ajuste se necessario.
# Conexao com o TimescaleDB local (docker compose). O valor abaixo e o default de dev.
DATABASE_URL=postgresql://oee:oee_dev@localhost:5432/oee_textil
# MQTT broker (docker compose, Fase 2). Valores abaixo sao os defaults de dev.
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
